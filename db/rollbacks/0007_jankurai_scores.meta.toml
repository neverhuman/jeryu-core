# jankurai:migration v1
id = "0007_jankurai_scores_rollback"
owner = "forge-api"
reviewer = "forge-api"
approval = "forge-api"
change_type = "contract"
safety = "online"
transaction = "required"
lock_timeout = "5s"
statement_timeout = "60s"
estimated_rows = 0
deploy_phase = "contract"
app_compatibility = "old_app_new_db"
rollback = "db/migrations/0007_jankurai_scores.sql"
roll_forward = "db/migrations/0007_jankurai_scores.sql"
backup = "sqlite .backup of forge.sqlite before apply (ops runbook: ~/.jeryu/backups/)"
restore = "restore the pre-apply .backup snapshot; scores are re-ingestable from CI runs or the backfill sweep"
irreversible = false
backfill = "none; drops the audit-score store only"
data_classification = "internal"
tenant_impact = "none"
verify = "PRAGMA table_info(jankurai_scores) is empty; app still serves /api/v1/repos"
tests = ["db-migration-analyze", "sqlite_store_round_trips_jankurai_scores"]
